# Set OPENROUTER_API_KEY in your shell or in an ignored .env.local file.
# Never commit a real OpenRouter key.
VSR_IMAGE_PROVIDER=openrouter
VSR_IMAGE_MODEL=openai/gpt-image-2
VSR_IMAGE_QUALITY=medium
# Set only if OpenRouter changes or documents a specific image endpoint.
# VSR_IMAGE_ENDPOINT=
